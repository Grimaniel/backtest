service: api-back-test
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev
  region: us-east-1

functions:
  getDatosCliente:
    handler: handler.getDatosCliente
    events:
      - httpApi:
          path: /datoscliente
          method: get
